<menu xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_turnFree"
          android:icon="@drawable/ic_action_pause"
          android:title="Ir para estado LIVRE"
          android:visible="false"
          android:showAsAction="ifRoom" />
    <item android:id="@+id/action_turnBusy"
          android:icon="@drawable/ic_action_play"
          android:title="If para estado OCUPADO"
          android:visible="true"
          android:showAsAction="ifRoom" />
    <item android:id="@+id/action_monitor"
          android:icon="@drawable/ic_action_person"
          android:title="View monitor details"
          android:visible="false"
          android:showAsAction="ifRoom" />
</menu>
